{
  "name": "product-master-hub",
  "version": "1.0.0",
  "description": "Enterprise Product Management Hub with Fiori Elements",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "@sap/cds-hana": "^2",
    "@sap/xssec": "^3",
    "express": "^4",
    "passport": "^0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/ux-specification": "^1.120.14"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "undeploy": "cds deploy --to sqlite:db/products.db",
    "build": "cds build",
    "deploy": "cds deploy"
  },
  "cds": {
    "requires": {
      "db": "sqlite",
      "auth": "mocked"
    },
    "features": {
      "fetch_csrf": true
    }
  }
}